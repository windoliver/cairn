[package]
name = "cairn-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
description = "Cairn verb layer, domain types, and error enums. No I/O, no adapters."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
toml = { workspace = true }
sha2 = { workspace = true }

[dev-dependencies]
async-trait = { workspace = true }
proptest = { workspace = true }

[features]
# Gated test helpers (e.g. `VerifiedSignedIntent::dangerous_unverified_for_testing`).
# Not enabled in any production build; intended for downstream test code
# that needs to construct domain types without going through the verifier.
test-helpers = []

[lints]
workspace = true
