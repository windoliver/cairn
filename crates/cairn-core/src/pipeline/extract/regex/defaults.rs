//! Built-in rule set. Spec §7. Populated in Task 8.

use super::rule::RuleSet;

/// Built-in rule set. Stub — Task 8 fills in the default rules.
#[must_use]
pub fn builtin_rule_set() -> RuleSet {
    RuleSet::empty()
}
