{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cairn.dev/schema/cairn.mcp.v1/verbs/lint.json",
  "title": "Cairn verb: lint",
  "x-cairn-contract": "cairn.mcp.v1",
  "x-cairn-verb-id": "lint",
  "x-cairn-capability": null,
  "x-cairn-auth": "read_only",
  "x-cairn-cli": {
    "command": "lint",
    "flags": [
      { "name": "write_report", "long": "write-report", "value_source": "bool" },
      { "name": "fix", "long": "fix", "value_source": "bool" }
    ]
  },
  "x-cairn-skill-triggers": {
    "positive": [
      "use to check vault health — contradictions, orphans, stale claims, missing concept pages"
    ],
    "negative": [
      "do NOT call per turn — run on a cadence (daily / on PR)"
    ],
    "exclusivity": "this is the canonical vault-health surface"
  },
  "type": "object",
  "$defs": {
    "Args": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "write_report": {
          "type": "boolean",
          "x-cairn-auth": "write_capability",
          "description": "When true, writes .cairn/lint-report.md."
        },
        "fix": {
          "type": "boolean",
          "x-cairn-auth": "write_capability",
          "description": "When true, resolves edge contradictions by invalidating lower-confidence live edges."
        }
      }
    },
    "Data": {
      "type": "object",
      "additionalProperties": false,
      "required": ["summary", "findings"],
      "properties": {
        "summary": {
          "type": "object",
          "additionalProperties": false,
          "required": ["total"],
          "properties": {
            "total":          { "type": "integer", "minimum": 0 },
            "contradictions": { "type": "integer", "minimum": 0 },
            "orphans":        { "type": "integer", "minimum": 0 },
            "stale":          { "type": "integer", "minimum": 0 },
            "ambiguous_edges": { "type": "integer", "minimum": 0 },
            "auto_resolved":   { "type": "integer", "minimum": 0 }
          }
        },
        "findings": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kind", "message"],
            "properties": {
              "kind":      { "type": "string", "enum": ["contradiction", "orphan", "stale", "missing_concept", "data_gap", "contradictory_edge", "ambiguous_edge"] },
              "message":   { "type": "string", "minLength": 1 },
              "record_id": { "$ref": "../common/primitives.json#/$defs/Ulid" },
              "severity": {
                "type": "string",
                "enum": ["info", "warning", "error"]
              },
              "entities": {
                "type": "array",
                "items": { "type": "string", "minLength": 1 }
              },
              "suggestion": { "type": "string", "minLength": 1 },
              "entity_id": { "type": "string", "minLength": 1 },
              "relation": { "type": "string", "minLength": 1 },
              "conflict_group_id": { "type": "string", "minLength": 1 },
              "candidate_edge_ids": {
                "type": "array",
                "items": { "type": "string", "minLength": 1 }
              },
              "chosen_edge_id": { "type": "string", "minLength": 1 },
              "fix_applied": { "type": "boolean" },
              "resolution_reason": { "type": "string", "minLength": 1 },
              "confidence_score": { "type": "number" },
              "confidence": {
                "type": "string",
                "enum": ["AMBIGUOUS", "INFERRED", "EXTRACTED"]
              }
            }
          }
        },
        "report_path": { "type": "string", "minLength": 1 }
      }
    }
  }
}
