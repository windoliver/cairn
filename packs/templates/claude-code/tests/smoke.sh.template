#!/usr/bin/env bash
set -euo pipefail

test -f ".claude/agents/{{subagent_id}}.md"
test -f ".claude/commands/{{command_id}}.md"
test -f ".claude/settings.json"
test -f "CLAUDE.md"
grep -q "{{pack_id}}" "CLAUDE.md"
