#!/usr/bin/env bash
set -euo pipefail

if [ -f "pack.json" ] && [ -f "agents/{{subagent_id}}.md" ]; then
  test -f "{{manual_fragment}}"
  test -f "commands/{{command_id}}.md"
  test -f "hooks/hooks.json"
  grep -q "{{pack_id}}" "{{manual_fragment}}"
  exit 0
fi

test -f ".gemini/agents/{{subagent_id}}.md"
test -f ".gemini/commands/{{command_id}}.md"
test -f ".gemini/hooks.json"
test -f "GEMINI.md"
grep -q "{{pack_id}}" "GEMINI.md"
